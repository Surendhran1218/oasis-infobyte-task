📌 Project Overview

This project implements a Client–Server Chat Application using Python.
The server is built using Flask, and the client communicates with the server using the Requests library.

The application demonstrates core networking concepts such as:

Client–Server Architecture
REST API Communication
HTTP GET & POST Requests
Real-time Message Handling
This version is fully compatible with Google Colab and runs without socket-related errors.

🏗 Architecture:

Client → HTTP Request → Server → Store Message → Respond → Client
The Client sends messages using HTTP POST.
The Server stores messages.
The Client retrieves all messages using HTTP GET.

📚 Libraries Used:

Flask (Server-side framework)
Requests (Client-side HTTP communication)
Threading (Run server in background)
Time (Delay handling)

About:

SURENDHRAN R
B.TECH AI&DS Student 
Passionate about App & Web Development
